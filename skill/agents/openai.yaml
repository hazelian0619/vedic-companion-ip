interface:
  display_name: "Vedic Companion IP"
  short_description: "Create safe companion choices from local chart facts."
  default_prompt: "Use $vedic-companion-ip to turn local chart facts into three safe Hatch companion candidates."
